"""
Task 06 — Classification Algorithms Comparison
Starter Template

This template provides a scaffold for the specific requirements of this task.
"""

import logging
from pathlib import Path

# Set up logging
logging.basicConfig(level=logging.INFO, format="%(asctime)s | %(levelname)s | %(message)s")
logger = logging.getLogger(__name__)

def main():
    logger.info("Starting Task: Classification Algorithms Comparison")
    # TODO: Implement multi-classifier comparison framework, threshold optimization, class imbalance handling
    # Key skills to demonstrate: Logistic Regression, Decision Trees, Random Forest, SVM, KNN — understanding when each works best, handling class imbalance (SMOTE, class weights), threshold tuning, ROC/PR curves
    
if __name__ == "__main__":
    main()
