"""
Task 14 — NLP Foundations — Text Classification
Starter Template

This template provides a scaffold for the specific requirements of this task.
"""

import logging
from pathlib import Path

# Set up logging
logging.basicConfig(level=logging.INFO, format="%(asctime)s | %(levelname)s | %(message)s")
logger = logging.getLogger(__name__)

def main():
    logger.info("Starting Task: NLP Foundations — Text Classification")
    # TODO: Implement text preprocessing pipeline, TF-IDF vectorization, multiple classifier comparison, text-specific evaluation
    # Key skills to demonstrate: Text preprocessing (tokenization, stemming, lemmatization, stopwords), BoW, TF-IDF, word embeddings (Word2Vec/GloVe), text classification with traditional ML, confusion matrix per class
    
if __name__ == "__main__":
    main()
